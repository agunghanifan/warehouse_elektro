<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warehouse Project</title>
</head>
<body>
    <nav>
        <a href="/categories">Category</a>
        <a href="/brands">Brand</a>
    </nav>
    <h1>Brand</h1>
    <table border="1">
        <thead>
            <tr>
                <th>ID</th>
                <th>Brand</th>
                <th>Product</th>
                <th>Category</th>
                <th>Stock</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(element => { %>

                <tr>
                    <td><%= element.id %></td>
                    <td><a href="/brands/<%= element.id %>"><%= element.name %></a></td>
                    <!-- tunggu product -->
                    <td>test</td>
                    <td>
                        <% element.Categories.forEach(e => { %>
                            <!-- this will become the link to each brand page -->
                            <a href="/categories/<%= e.id %>"><%= e.name %></a>
                        <% }) %>
                    </td>
                    <!-- tunggu product -->
                    <td>test</td>
                     <td>
                         <a href="/brands/delete/<%= element.id %>">delete</a>
                    </td>
                </tr>

            <% }) %>
        </tbody>
    </table>
</body>
</html>